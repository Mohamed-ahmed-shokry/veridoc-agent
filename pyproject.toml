[project]
name = "veridoc"
version = "0.1.0"
description = "Invoice and purchase-order verification service"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
veridoc = "veridoc:main"

[build-system]
requires = ["uv_build>=0.9.13,<0.10.0"]
build-backend = "uv_build"
